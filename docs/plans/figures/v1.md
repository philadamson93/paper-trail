# Advisor-pitch figure — v1

The figure for `docs/plans/advisor-pitch.md`. Contains the caption paragraph intended for the pitch prose, and the image-generation prompt ready to paste into nano-banana. A rendered version lives alongside at `v1.jpg`.

## Caption

**Figure 1 — Isolation around `/paper-trail` (the agentic program), evaluated by a filtering dispatcher.** *Left:* one `--bare` invocation of `/paper-trail` (tag v1), walled against ambient host context; invoked N times per revision, one claim per invocation. *Middle:* a deterministic Python dispatcher — filtering locked at write-time — that invokes the program, collects per-item verdicts, and returns per-split signal to the agentic optimizer (train: per-sample inputs + labels + traces; val: macro F1 only). *Right:* the agentic optimizer, a Claude Code session co-resident with the ambient host context (`CLAUDE.md`, auto-memory, hooks, plugins, dynamic system-prompt content) and using it by default — no `--bare`. *Below:* the test set sealed in its own locked container, accessed only once at terminal evaluation. A stand-in agent outside the agentic program's wall supplies human-like input if the program's normal operation takes a real user. A human drives the agentic optimizer every M iterations (M=1: in-loop; M→∞: autonomous).

---

## Image-generation prompt

```
A clean, minimal technical architecture diagram. Landscape orientation,
single integrated composition, muted palette (slate gray, soft teal,
warm beige), white background. No outer frame around the whole figure.
The composition shows three walled regions arranged horizontally across
the middle of the figure — the agentic program on the left, a
dispatcher in the middle, the agentic optimizer on the right — plus a
sealed test lockbox below the dispatcher and a stand-in agent below
the agentic program.

THE AGENTIC PROGRAM (left side, primary visual emphasis). A closed
rounded-rectangle with a THICK wall on all four sides, carrying a
subtle hatched texture — the strongest line in the figure. Directly
above this region, a small italic label reads: "Claude Code session".
The region itself is labeled at its upper edge in clear text:
  "/paper-trail  —  tag v1, --bare"
Near the upper-right corner of the region, overlaid on or adjacent to
the wall: a small circular-arrow icon — like a replay or repeat symbol
(a circle with a curved arrow indicating repetition). NOT a fast-
forward / double-chevron symbol. Next to this icon, a small label:
  "× N per revision"
Inside the region, compact content:
  - A small inline list in the center, separated by middle dots, naming
    the committed tag contents:
    "slash command · subagents · tool permissions · MCP config · prompts".
Below the agentic program, OUTSIDE its wall, a small pill-shaped node
labeled "stand-in agent". A short arrow runs from the stand-in agent
UP into the agentic program (crossing the bottom wall cleanly), labeled
"human-like input (if required)".

THE DISPATCHER (middle, secondary emphasis). A closed rounded-rectangle
notably smaller than the agentic program and the agentic optimizer.
Walls on all four sides, MEDIUM thickness. Inside:
  - A hexagonal or gear-shaped node (visually distinct from pill-shaped
    agent nodes) labeled "dispatcher".
  - Directly beneath the node, two small lines of text:
    "deterministic Python"
    "filtering locked at write-time"

THE AGENTIC OPTIMIZER (right side, notably LARGER than either other
region — it needs to comfortably hold the clustered ambient context and
the human). A closed rounded-rectangle with walls on all four sides,
MEDIUM thickness. Directly above this region, a small italic label
reads: "Claude Code session". The region itself is labeled at its upper
edge:
  "agentic optimizer"
Inside the region:
  - A pill-shaped node labeled "agentic optimizer" in the upper-center.
  - Five small labeled icons clustered around the node (above, left,
    right, below), each at full opacity:
      · a scroll icon labeled "CLAUDE.md"
      · a folder icon labeled "auto-memory"
      · a gear icon labeled "hooks"
      · a puzzle-piece icon labeled "plugins"
      · a tag/banner icon labeled "dynamic system prompt"
    These icons have NO arrows connecting them to anything. Their
    clustering around the agentic optimizer is the point: the optimizer
    uses this ambient host context by default.
  - A small human-silhouette icon labeled "human" in the lower-right
    corner of the region. A short curved arrow runs from the human
    up-and-left into the "agentic optimizer" node, labeled "M", with a
    small subtitle beneath the arrow reading:
    "M=1: in-loop · M→∞: autonomous".

SEALED TEST LOCKBOX (below the dispatcher, its own separate element).
A small closed rounded-rectangle with a prominent padlock icon rendered
on or alongside one of its walls. Labeled: "test (sealed)". Small
subtitle beneath: "unsealed once at terminal evaluation". Visually
isolated — not nested inside any other region.

FLOWS — drawn in a contrasting teal accent color except where noted:

  1. INVOKE / REVISION — a single arrow originating at the "agentic
     optimizer" node, running LEFTWARD, passing visibly through the
     "dispatcher" node, then terminating inside the agentic program
     (crossing the walls of the optimizer region and the agentic
     program region cleanly). Label near the optimizer-to-dispatcher
     segment: "invoke · revision".

  2. PER-ITEM VERDICT — a thin arrow from inside the agentic program,
     running RIGHTWARD, terminating at the "dispatcher" node (crossing
     walls cleanly). Label: "per-item verdict × N".

  3. TRAIN SCORING RETURN — a THICK solid teal arrow from "dispatcher"
     running RIGHTWARD to "agentic optimizer", crossing walls cleanly.
     Label: "train · per-sample + labels + traces".

  4. VAL SCORING RETURN — a THIN solid teal arrow from "dispatcher"
     along a slightly lower path, running RIGHTWARD to "agentic
     optimizer", crossing walls cleanly. Label: "val · macro F1 only".

  5. TEST ACCESS (dashed) — a DASHED gray line from "dispatcher"
     running DOWNWARD to the sealed test lockbox. Line ends at the
     padlock, NOT at an arrowhead. Label alongside: "terminal
     evaluation only".

No other arrows exist in the figure. No OUT-icon arrows, no
extraneous connectors.

BRANDING (subtle, lower-right corner). A small Anthropic logo (clean
schematic mark — the burst/star-like shape) rendered at modest size in
the lower-right corner of the figure, accompanied by small text reading
"Claude Code" set in a subtle parenthetical style. Tasteful credit, not
intrusive.

Style: clean line weights, diagrammatic, minimal. Visual emphasis
ordering:
  (1) The agentic program's thick wall (primary — `--bare` isolation).
  (2) The train and val scoring arrows between dispatcher and agentic
      optimizer (the dispatcher's filtering discipline).
  (3) The sealed test lockbox.
Text is minimal — labels only, plus the two small italic "Claude Code
session" labels above the agentic program and the agentic optimizer.
All nodes are small pill-shaped or hexagonal markers, never humanoid
or blocky. No cartoons, no emoji, no photorealism, no busy decorative
elements. White background throughout.
```
