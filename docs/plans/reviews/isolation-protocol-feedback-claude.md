Reference: docs/claude_ops.md

# Feedback: The isolation protocol (Fresh Claude Code review)

## Verdict

**Revise.** The diagnosis is excellent and NF2/NF3/NF4/NF6/NF7 and the Task-subagent claim all check out against current code — but four things must change before implementation: Phase 0b as written would score **every affected batch 0.0** (it collides with the Scorer's 100%-coverage rule and mis-cites the current status it replaces), the plan's own measured baseline was produced by a **silently-skipped** engine block using the wrong env var in its own recipe (real gate is 444/445, not 258/259), Phase 1a relocates the judge's cwd to a **sibling of `~/.paper-trail/gold/`** while `inner_scope_problem`'s forbidden set is repo-relative only, and the central "mounts are the only hard barrier" claim never examines dropping `--dangerously-skip-permissions`, so Phase 3 may be mis-scoped as the seal.

---

## Critical Gaps

- **Severity: Critical | Phase 0b's `infra_error` zeroes the whole batch.** The Scorer requires total coverage: `complete = scored["n_total"] == requested and unresolved == 0` and returns `result(0.0, breakdown)` when incomplete. Any record whose status is not `("ok", "invalid_output")` is `continue`d out of the pairs list. So a per-claim `status="infra_error"` — the plan's stated design, "never a scored data point" — drops the claim, breaks coverage, and returns **0.0 for the entire batch**. At the observed 2.3% no-task rate, P(≥1 in a 50-claim batch) ≈ 69%, so this fires on most batches. This is verbatim the 2026-09-07 failure the code comment at `adapter.py:919-930` was written to prevent ("93 of 100 claims were fine and every number came back 0.0"), and the plan re-introduces it while quoting the surrounding lines. | Evidence: `experiments/sarol-2024/optimizer/adapter.py:1382`, `:1414`, `:1428-1433`, `:919-930` | **Required fix:** decide and state the coverage policy explicitly — (a) re-dispatch the claim (bounded retry) so coverage holds, (b) decrement `requested_count` for infra-failed claims (a contract change to the run manifest that the Scorer's denominator reads at `:1371`, and which must be visible in the breakdown), or (c) keep it scored but tag it so the corpus filter excludes it while the denominator does not. Whichever is chosen, add a gate that asserts a batch containing one infra_error still produces a non-zero metric.

- **Severity: Critical | Phase 0b mis-cites the status it is replacing, pointing the implementer at the wrong branch.** The plan says "Today it becomes `program_error` (`adapter.py:900-903`)". `:900-903` is the **exit-code** branch. A no-task session that exits 0 and writes nothing goes to `validate_sarol.validate_file(missing_path)` → `ValidationResult(ok=False, violations=["UNREADABLE:..."])` → `record["status"] = "invalid_output"` at `:930` — which is **scored as a miss** and merged into `error_class_counts`. Nine of ten releases reporting `n_invalid=0` is inconsistent with that and deserves its own check. An implementer following the citation will edit the `res.exit_code != 0` branch, converting *genuine* program failures into `infra_error`. | Evidence: `adapter.py:900-903` vs `:907-930`; `validate_sarol.py:374-377` | **Required fix:** cite `:907-930`, and specify the discriminator precisely — the fix keys on the `UNREADABLE:` violation prefix (file absent) and nothing else, so a malformed-but-present verdict stays `invalid_output`.

- **Severity: Critical | The plan's stated baseline is an inert measurement, and the plan enshrines the inert recipe.** The plan's recipe exports `PYTHONPATH=~/.paper-trail/engine-82f547d`. `adapter.engine_path()` reads **`AGENTIC_LABEL_OPT`**, defaulting to a Mac-only path (`~/Documents/Misc/Projects/agentic-label-opt`). Run as the plan prescribes, adapter prints `PASS engine not found at /home/philadamson/Documents/... -- engine-facing checks SKIPPED` and reports **30/30**; run with `AGENTIC_LABEL_OPT=~/.paper-trail/engine-82f547d` it reports **150/150**, and dispatcher goes 48/48 → **114/114**. The real total on this branch is **444/445**, not 258/259 — 186 checks silently skipped. The plan then declares the diagnosis memory's "adapter 150 / dispatcher 114" a *phantom* it is protecting the implementer from. It is the true number. Worse: the precedent the plan cites for item 2 (`empty_checkout` at `adapter.py:2118`) lives **inside** the skipped engine-guarded block, so the plan cites as proven an assertion its own baseline never ran; and every engine-schema check — the exact surface Phase 2 touches — is in the skipped set. A plan whose thesis is "inert, unchecked assertions caused the defect" cannot ship this recipe. | Evidence: `adapter.py:102`, `:117-129`, `:190`, `:2110-2130`; measured on `sarol-optimizer-concurrent` @ `591eb02` | **Required fix:** replace the recipe with `export AGENTIC_LABEL_OPT=~/.paper-trail/engine-82f547d`, re-table the baseline at 444/445, retract the phantom-baseline paragraph, and add a Step-0 assertion that the engine block is **not** skipped (the "skip" is currently a `PASS`, which is the same inertness defect one layer down).

- **Severity: Critical | Phase 1a moves the judge's cwd next to the gold tree, and `inner_scope_problem` cannot see it.** `~/.paper-trail/` holds `gold/`, `benchmarks/`, `runs/`, `engine-82f547d/`. The plan materializes the minimal checkout at `<run_root>/judge-checkout/`, i.e. under `~/.paper-trail/runs/…` — **two `..` hops from `~/.paper-trail/gold/sarol-2024`**. Findings §5's "gold lives outside both the repo tree and the staging tree, not reachable by accident" is true of the *repo* cwd and stops being true the moment cwd moves under the gold tree's parent; "survey the parent of my working directory" is a far more natural ambient move than "read an absolute path in a repo nobody mentioned". Meanwhile `inner_scope_problem(working_checkout, forbidden={iter/, optimizer/findings/, meta-learnings.md, optimizer/context/})` is an entirely **repo-relative** forbidden set and would pass this checkout happily. Note also NF6 under-states itself: `_gold_root()` falls back to `~/.paper-trail/gold` with **no env var set at all**, so scrubbing `PAPER_TRAIL_GOLD_DIR` from the child env buys nothing against gold — the path is hard-coded and deterministic. | Evidence: `experiments/sarol-2024/scripts/parse_verdict.py:47-51`; `ls ~/.paper-trail/`; plan §Phase 1a | **Required fix:** put the minimal checkout under a dedicated gold-free root (Rule 3's own `/tmp/pt-eval-wd-<uuid>` shape, which also guarantees no `.git`), add `~/.paper-trail/gold` and `~/.paper-trail/benchmarks` to `inner_scope_problem`'s forbidden set as absolute-path entries, and plant a **gold** sentinel in V4a — it is the highest-value secret in the system and is currently the one thing V4a does not probe.

- **Severity: Critical | The "mounts are the only hard barrier" thesis never examines the flag that creates the problem.** The plan's premise — `--dangerously-skip-permissions` removes the prompt that would gate an out-of-tree read — is true. But that flag is a **retained choice**, not a constraint, and the CLI ships a directly relevant alternative: `--permission-prompts none` ("nobody: anything that would prompt is denied automatically; the permission mode still decides everything else") plus a non-bypass `--permission-mode`, optionally with `--allowedTools` / `--disallowedTools`. Under that combination an out-of-tree read is **denied**, not allowed, and cwd + `--add-dir` become a real app-layer boundary. The plan never asks whether the judge needs permission bypass, so the conclusion "container mounts are the only hard barrier available" is asserted rather than established — and Phase 3, the most expensive and most uncertain phase, is scoped as *the seal* on that unestablished basis. V0a is a valid test of the narrow proposition it states, but it probes only the bypass arm and therefore cannot change any decision: the answer is already in the flag's own help text. | Evidence: `claude --help` (`--permission-prompts`, `--permission-mode`, `--allowedTools`, `--disallowedTools`); `adapter.py:711-731` | **Required fix:** make V0a **two-armed** — arm A as written, arm B with `--permission-prompts none` + a non-bypass permission mode + an `--allowedTools` set sufficient for the judge's real tool use, measuring both (i) does the out-of-tree sentinel read get denied and (ii) does the judge still produce a valid verdict for its staged claim. Pre-encode the fork: if arm B seals and the judge survives, Phase 3 demotes from "the seal" to defence-in-depth and OQ3 resolves much more cheaply.

---

## Failure Modes

- **A cache hit makes V1a pass vacuously.** `CachingRunner` wraps `SarolRunner` in `build_components` and keys on `(program_digest, input_ref, split)`; a second run of the same claim under the same program returns the cached `RunArtifacts` **without dispatching**, so the archive-then-assert-absent code never executes and V1a ("dispatch the same claim twice") records a pass it did not earn. | Add: state that V1a runs against `SarolRunner` directly (or `CachingRunner(enable_cache=False)`), and add a companion check that the archive fires on a genuine cache **miss** with an unchanged claim id. | Evidence: `dispatcher.py:306-346`

- **`inner_scope_problem` fail-closed at `dispatcher.py:549-551` breaks seven existing callers.** That line is inside **`build_components`**, not `run_optimization` (the plan says "`run_optimization`'s preflight at `dispatcher.py:549-551`" — pick one; the line number and the function name disagree). `build_components` is called with the `working_checkout=adapter.REPO_ROOT` default from six selftest sites (`:1253`, `:1399`, `:1404`, `:1416`, `:1421`, `:1603`) and `scripts/run_baseline.py:121` passes `working_checkout=store.repo_root` **explicitly**. A fail-closed raise there turns all seven red — and `run_baseline.py` is what Plan A's owed baseline recut runs, so this plan's gate would refuse the very recut it contracts Plan A to perform on the post-isolation instrument. | Add: name the exact locus, enumerate the seven call sites as lockstep updates, and say how `run_baseline.py` is meant to satisfy the gate (its own minimal checkout, or an explicit opt-out flag with a stated rationale — not a silent bypass).

- **Phase 2's hash gate as specified is a tautology.** `SarolReleaseBuilder()` is constructed with **no arguments** at all five sites, taking the `"sarol-2024"` default. If `build_components` passes the computed hash into that constructor and `run_optimization` then compares "the value the release builder would stamp" against "the computed one", the gate compares a value to itself and can never fail — the same inertness as the label it replaces. | Add: reuse the pattern this repo already proved — `canary.py`'s **committed pin** ("a guard whose reference value is not under version control is not a guard"). Commit an `isolation-pin.json` recording the expected isolation hash and refuse when the computed value diverges, with a re-pin path. State V3's Expected against the *pin*, not against the builder. | Evidence: `adapter.py:1499-1503`; `dispatcher.py:582`, `:1019`, `:1089`, `:1205`; `adapter.py:2038`; `canary.py:16-36`, `:59`, `:118-151`

- **Containerizing swaps the instrument under the measurement.** `isolation/Dockerfile:25` pins `CLAUDE_CODE_VERSION=2.1.218`; the installed CLI on this box is **2.1.267**. OQ3's recommended "containerize for real" therefore changes the judge's CLI by 49 patch releases at the same moment the plan owes Plan A a clean post-isolation baseline. Nothing in `runtime_pins`, V0c, or V5 notices. | Add: pin the image's CLI to the host version (or vice versa) as an explicit deliverable, record the CLI version in the run manifest beside `model`/`profile`/`retrieval_k`, and make V5 assert version parity.

- **Phase 3's mount manifest has no network policy, and `network_policy` is a required argument.** `build_docker_cmd_prefix` takes `network_policy` with no default; `"none"` (the shape the existing negative controls use) would deny the judge its own API calls. The engine already documents paper-trail's answer as the **`"open"`** profile with `adc_path=None` and `CLAUDE_CODE_OAUTH_TOKEN` in env. The plan specifies mounts in detail and network not at all. | Add: name `network_policy="open"` + `adc_path=None` explicitly, and file the paper-trail host-allowlist profile (`isolation/README.md`: "crc's and paper-trail's own profiles … still unbuilt") as a named follow-up rather than leaving egress unmentioned. | Evidence: `isolation/docker_prefix.py:444-464`; `isolation/open_profile.py:68-72`

- **`--bare` exists and the tracker already prescribes it.** `claude --bare` is documented as "skip hooks, LSP, plugin sync, attribution, **auto-memory**, background prefetches, keychain reads, and **CLAUDE.md auto-discovery**" — one flag covering the plan's entire last table row, whose two env-var names the plan itself flags as unverified. `NEXT.md:461` already names `claude --bare --print /sarol-eval-item …` as the intended invocation. **Caveat that must be stated, not glossed:** `--bare` refuses OAuth/keychain auth (strictly `ANTHROPIC_API_KEY` or `apiKeyHelper` via `--settings`), and Rule 3 records that `--bare`-style default toolsets **omit `Task`** — which OQ1 option (i) requires, since the judge is a Task subagent. | Add: put `--bare` in V0b's matrix as a candidate for that row, with the auth and `--tools default` caveats named.

---

## Contract Checks

- **`~/code/agentic-label-opt` (shared engine; consumed by rad-eval / crc / MedVAL) — `isolation/docker_prefix.py`.** The additive/deferred split is right in principle, but **Phase 3a's engine change looks unnecessary.** `build_docker_cmd_prefix` already exposes generic `extra_ro_mounts: Sequence[tuple[Path, str]]`, `writable_mounts: Sequence[tuple[Path, str]]`, `workdir`, `env`, `container_user`, `adc_path`, and an explicit `edit_agent_mounts=None` shape for "a consumer with no editing agent at all (e.g. crc)". The judge principal — `{staging rw, spec_root ro, minimal checkout ro}` — is expressible **today** with those params and zero engine diff. Adding a second named principal dataclass beside `EditAgentMounts` is a shared-repo change buying naming, not capability. Recommend: express the judge principal consumer-side with the existing generic params, and if a named `JudgeMounts` is still wanted, land it as a pure refactor **after** the consumer proves the shape. That converts Phase 3a from "additive change to a repo three siblings consume" to "no shared-repo change", which is strictly better for the cross-repo contract and consistent with the plan's own NF4 reasoning. | Evidence: `isolation/docker_prefix.py:444-464`, `:96-112`, `:482-503`
- **`isolation/negative_control.py` (same shared repo).** Adding a sentinel-read control is genuinely additive and correct. But an "explicit **deny** list" as a *config field* is inert by construction — Docker mounts are allow-only, so a deny list only means something if it is the input to a **runtime probe**. Say so in the plan: the deny list's contract is "every entry in this list is probed by V4a and must fail", or drop the field and probe the paths directly. As written it is a decorative label of exactly the `optimizer_isolation_hash` shape.
- **Deferred upstream items are named but under-specified to survive.** "`schemas.py` hash validation" and "the engine-side consumer-did-not-containerize refusal gate" are recorded in *Landing & cleanup* prose only. Each needs, in the plan: the file+line seam (`engine/schemas.py:207-233` — the same `__post_init__` that already validates `phase`), the blockers by name (root-level `adapter.py:217`/`:225` pass `"fake"`; `tests/test_envelope_validation.py:76`/`:84` pass `"h"`), and the migration that unblocks them (give the reference adapter and both tests a real computed hash first). Without that, the follow-up is a sentence, not a task. **Also fix the citation: the reference adapter is repo-root `adapter.py`, not `engine/adapter.py`** — `engine/adapter.py` does not exist at the pinned SHA. | Evidence: `~/.paper-trail/engine-82f547d/adapter.py:217`, `:225`; `engine/schemas.py:207-233`
- **In-repo: `.claude/commands/sarol-eval-item.md` prompt-slot contract.** Verified sound: 8 slots, byte-identical across `sarol-optimizer-concurrent` and Plan A's `0f43ba9`; Plan A has **not** touched the command file. Also verified: the dispatch prompt's `{{spec_root}}/src/specs/verdict_schema.md` reference (line 101) resolves — `src/specs/verdict_schema.md` **is** in the program-v0 manifest, so it materializes into spec_root and the minimal checkout does not remove a file the judge currently reads. Good; the plan's rendering contract is stable.
- **Coordination with Plan A is under-reported.** The plan states it ran two checks and found no collision. Plan A @ `0f43ba9` **does** modify `experiments/sarol-2024/optimizer/adapter.py` (+12/-6) and `profiles.py` (+2/-2) — both files this plan modifies. The overlap is benign (all of Plan A's adapter hunks are inside `_selftest()` at `:1665-1775`, exactly where this plan adds its own new checks; profiles.py is the 8→9 entry-count constant), but "verified, no collision" is the wrong summary. State the overlap and that it is `_selftest()`-local. It also means the 9-entry manifest / `combined_hash` `8d8fe097` lands *after* this plan, so V1c's "`combined_hash` still covers the source" must be written against whichever manifest is live, not a pinned literal.
- **`docs/claude_ops.md` §Environment is stale** ("no build step, no runtime, and no test suite"); this plan's entire verification rests on a 445-check selftest suite. Per source precedence the plan is right and the ops doc is wrong — worth one line in *Files to Modify* rather than leaving the contradiction.

---

## Modularity vs. YAGNI

- **Two new modules (`isolation.py`, `dispatch_prompt.py`) in a flat one-module-per-concern dir, no new package** | Plan's choice: two modules, no package | Agree, no change needed. The directory convention (`canary.py`, `profiles.py`, `sampling.py`, `evidence_producers.py`, `validate_sarol.py`) is real and two files do not justify a package. Keeping prompt delivery out of `isolation.py` is the right cut. Also correct not to grow `adapter.py` past ~2,900 lines.

- **`dispatch_prompt.py` re-implements slot sourcing and integrity checks that already exist** | Plan's choice: a fresh module that reads `ledger/evidence/<claim_id>.json` + `staging_info.json`, runs `CLAIM_ID_MISMATCH` / `CLAIM_TEXT_MISMATCH`, substitutes, writes | **This is a genuine reuse miss.** `evidence_producers.py:210-216` *already* reads `staging_info.json`, takes `claim_text_normalized`, and writes the very envelope the renderer then re-reads — under the `retrieval` profile it is the same Python process that produced both sides of the join, so the `CLAIM_TEXT_MISMATCH` check is comparing a value to its own source and cannot fail. Separately, `validate_sarol.py:266` already emits a `CLAIM_ID_MISMATCH:` violation string; a second, independently-formatted `CLAIM_ID_MISMATCH` in a new module gives one error name two vocabularies — the classic drift shape. | Recommendation: factor a single slot-source helper (`staging_info` + envelope read, one place) shared by the producer and the renderer; state that the two integrity checks are load-bearing only for the `agentic`/`paperclip` profiles where the extractor (not the producer) writes the envelope, and that under `retrieval` they are a tautology retained for profile parity; and reuse `validate_sarol`'s violation-code vocabulary rather than minting a parallel one.

- **`inner_scope_problem` modelled on `val_isolation_problem`** | Plan's choice: same shape, new pure predicate in `isolation.py` | Correct and well-argued — but it is *modelled on*, not *reusing*, and it should reuse two things: `val_isolation_problem`'s containment idiom (`resolve()`, `path == other or other in path.parents`) rather than re-deriving path containment, and the `_raises_valueerror` selftest helper at `dispatcher.py:455` that the VAL check's own negative control uses. Also fix the forbidden set: it is repo-relative only, while Rule 1's actual list is "gold labels, claim row-ids, split names, paper buckets, or **raw benchmark data**" — `~/.paper-trail/gold` and `~/.paper-trail/benchmarks` are absolute and currently uncovered (see Critical #4).

- **Per-principal mount manifest vs `EditAgentMounts`** | Plan's choice: add a sibling judge principal to the shared engine | Modular alternative: express the judge with the **existing** generic `extra_ro_mounts`/`writable_mounts`/`edit_agent_mounts=None` params — zero engine diff, and the realistic second use case (a crc- or rad-eval-side judge principal) does not exist yet | Recommendation: consumer-side with existing params now; revisit the named dataclass when a second consumer actually needs it.

- **Sentinel negative control vs `isolation/negative_control.py`** | Plan's choice: a new control plus "an agentic probe", described as "the largest single unknown in this plan" | The existing `run_probe(scenario, *probe_command)` is a direct `docker run … cat <path>` — which is a **complete** proof of a mount seal, because a mount either exists or does not. The plan bundles the seal proof and the agentic-liveness proof into one gate (V4a: "every read fails **and** the judge still produces a valid verdict"), which makes the cheap, decisive half hostage to the container-auth unknown. | Recommendation: split into **V4a-seal** (non-agentic, reuses `run_probe` + `_build_common_fixture`, runs today, no auth question, gates everything downstream) and **V4a-live** (one real `claude -p` in-container, gated on V0c). This also deflates the "largest unknown" framing: `isolation/open_profile.py:68-72` already documents paper-trail's in-container auth as `CLAUDE_CODE_OAUTH_TOKEN` + `adc_path=None`, and the ALO Phase C run is prior art. | Evidence: `isolation/negative_control.py:44-58`, `:102`, `:168`, `:266`; `tests/test_isolation_negative_control.py:173-180`

- **`Invoker` stays 3-arg; bind `env`/`cmd_prefix` via `functools.partial`** | Plan's choice: bind, don't widen | Agree, and the reasoning ("one seam, both uses") is sound. One thing to state: with `env` bound at construction, the spy invokers in the selftests observe `(cmd, cwd, timeout)` only and therefore **cannot assert on the child env** — yet V2c's core assertions are about env (`PAPER_TRAIL_GOLD_DIR` absent, allowlist-only, `CLAUDE_CONFIG_DIR` differs across invocations). Name how V2c reaches the bound values (assert on the `functools.partial`'s `keywords`, or expose the resolved env as a Runner attribute), or the gate is unwritable as specified.

---

## Verification Gaps

- **V2c's headline assertion is not meaningful as phrased.** "`$REPO` **absent** from the argv" cannot hold: staging lives at `REPO_ROOT/experiments/sarol-2024/staging/<claim>` (`adapter.py:559` resolves `staging_dir` against `REPO_ROOT`; `.gitignore` `/experiments/*/staging/`), so the repo path is a **prefix of every `--add-dir` entry**. As written the check either false-fails always or gets weakened to a substring test that proves nothing. Restate as: *no `--add-dir` entry equals or is an ancestor of `$REPO`*, and add the positive form — the entry set is exactly `{<staging>/<claim_id>, <materialized spec_root>}`.
- **V4a does not probe gold.** It plants sentinels in `release_train.json`, `findings/iter-1.md`, `meta-learnings.md` — the exposure the findings doc documented — but not `~/.paper-trail/gold/sarol-2024`, which is the actual crown jewel and, after Phase 1a, a sibling of the judge's new cwd. Add a gold sentinel and a `~/.paper-trail/benchmarks` sentinel.
- **V0b's Stop is right; its Expected is not falsifiable for the two env vars.** "each env var either confirmed effective or replaced by a mechanism that is" has no observable. Give it one: run a session in a directory containing a `CLAUDE.md` and a `.git` with dirty state, with and without each var, and assert on the presence/absence of the project-context and git-status sections in the trace. (This is also how to settle whether `--bare` or `--exclude-dynamic-system-prompt-sections` alone suffices.)
- **The demonstrated vector is mis-attributed, which mis-ranks the fixes.** The plan calls the "auto-memory / project-`CLAUDE.md` loading" row "the highest-value single change in this plan for the *demonstrated* vector … independent of cwd". But the quoted session enumerated specific filenames *"based on the git status"* — that is the **dynamic system-prompt git-state section**, which is cwd-dependent and which Rule 3 kills at the source ("no `.git/` in cwd means no branch/uncommitted-files section in the prompt; empirically verified 2026-04-23"). Both channels fired; the one that produced the filename list was git-state, not CLAUDE.md. Re-rank accordingly, and make V2b assert the minimal checkout contains **no `.git` and no `CLAUDE.md`** (today V2b's "nothing else that is not required" is too loose to catch either).
- **No gate detects a silently-skipped gate.** The engine block's skip currently prints `PASS`. Given Critical #3, add a Step-0/Step-1 assertion that the engine-facing checks **ran** (e.g. assert the adapter total is ≥150 / that `_import_engine()` succeeded), and consider making the skip print `SKIP` rather than `PASS` upstream in the selftest harness. This is the cheapest single instrument fix in the whole plan and it is the plan's own thesis applied to itself.
- **V5's Stop has no floor on the metric.** "zero no-task sessions, zero stale reads, cost within 25%" — but the most likely real-world outcome of Phase 0a+0b is a *metric shift* (removing 61% self-consistency should push VAL **down**), and V5 would report that as a pass. Add: record the metric and state that a large drop is the **expected** signature of removing contamination, not a regression — otherwise the first person to run V5 will read a lower number as a bug and revert the fix.
- **Docker verification recipe (real hardware).** Verified for the author: `hostname` = `phil-sllm-01`, `docker info` succeeds, user is in the `docker` group, so `docker_available()` returns True and Phase 3 can run here. State the concrete commands (`AGENTIC_LABEL_OPT=… ~/.local/bin/python3.13 -m pytest tests/test_isolation_negative_control.py`), that the image must be built first (`isolation/Dockerfile`, `docker build --build-arg CLAUDE_CODE_VERSION=…`), and that the 104/104 figure is the whole smoke suite, not this file (which has 15 tests).

---

## Handoff Readiness

- **Where does `inner_scope_problem` actually get raised?** The plan says `run_optimization`'s preflight *and* `dispatcher.py:549-551`, which is inside `build_components`. → **STATE DIRECTLY:** the function name, the line, and the seven existing callers that must change with it (`dispatcher.py:1253`, `:1399`, `:1404`, `:1416`, `:1421`, `:1603`; `scripts/run_baseline.py:121`).
- **Which sister file does each new module model?** `isolation.py` is described as "matching the convention" without naming a model. → **POINT AT:** `dispatcher.py:465-490` (`val_isolation_problem`) for the predicate shape and `canary.py` for the committed-pin/refusal shape that Phase 2 needs.
- **How does a claim that fails closed still satisfy coverage?** Unanswered — see Critical #1. → **STATE DIRECTLY:** the policy, the field(s) that change in the run manifest (`requested_count` at `adapter.py:1006`), and every reader of those fields (`SarolScorer` at `:1371`, `:1414`; the release breakdown; `run_hillclimb_vm.sh`'s post-run print).
- **What exactly does the minimal checkout contain, and where does it live?** "only `.claude/commands/sarol-eval-item.md`" plus a path under `<run_root>/`. → **STATE DIRECTLY:** the full fileset, the parent directory (and that it must be gold-free — see Critical #4), and that it must contain no `.git` and no `CLAUDE.md`.
- **What is the judge's auth mechanism?** The env-allowlist row says "Claude auth" without naming it, yet this single choice determines the allowlist, whether `--bare` is usable, and V0c's whole shape. → **STATE DIRECTLY:** `CLAUDE_CODE_OAUTH_TOKEN` (per `isolation/open_profile.py:68-72` and Rule 3's own note that `--bare` refuses keychain OAuth), or say why not.
- **Is the *driver* containerized, or only the judge?** Phase 3 says "a `cmd_prefix` bound onto `SarolRunner`", which wraps the driver invocation and hence the subagent too — correct, but never stated. → **STATE DIRECTLY:** one line; and note that a wholesale `DockerRunner` replacing `SarolRunner` must preserve the `CachingRunner` + `BudgetGuard` wrapping built at `dispatcher.py:553-570`.
- **Out-of-scope statement:** present and good (corpus accounting, post-run assertion, measurement redesign, artifact rewrite, judge-model A/B). Keep it. Add the retired PreToolUse/tag-scoped-`settings.json` control (below) so it does not fall through the same crack.
- **NF1 silently retires a specified control.** NF1 correctly establishes there is no `settings.json` anywhere (verified: `git ls-files | grep -i settings` empty, `find . -name 'settings*.json'` empty, `.claude/` holds only `commands/ prompts/ scripts/ skills/ specs/`) and correctly concludes the wide cwd bought no hook stack. But `NEXT.md:463` says "**Commit** any per-experiment `.claude/settings.json` / MCP config to the repo (captured by the paper-trail version tag — do NOT let these live in user-global settings)", and Rule 3's stack passes `--settings <tag-scoped>` and `--agents <tag-scoped>`. The requirement was that a tag-scoped settings file **should exist**; NF1 converts "it doesn't" into "we don't need one" and drops the whole enforcement layer with no decision recorded anywhere. → **STATE DIRECTLY:** either author the tag-scoped settings + PreToolUse hook as a phase, or list it as deliberately out-of-scope with the reason (superseded by mounts / by the permission-mode option in Critical #5). Given this plan's own root-cause finding is *specified-but-unimplemented, then reasoned away*, dropping it in a `Consequences:` clause is the failure mode reproducing itself.
- **Rule-3 table omits three pieces of the canonical stack.** `--tools default` (Rule 3: "enables `Task` … which `--bare`-style default toolsets omit" — required for OQ1 option (i), where the judge *is* a Task subagent), `--agents '<json>'`, and `--settings <tag-scoped>`. All three are unused today, so their absence is defensible — but say so, since the table presents itself as Rule 3 with only its *scope* piece inverted. | Evidence: `docs/plans/experiment-sarol-eval-arm-isolation.md:107-115`
- **Dangling citation:** `engine/adapter.py:217`, `:225` — no such file at the pinned SHA; the reference adapter is repo-root `~/.paper-trail/engine-82f547d/adapter.py` (line numbers correct). Also `smoke.md:98` vs `:100` is already self-corrected in the plan — good, keep that footnote.
- **Landing plan:** branch / worktree / landing gate / merge sequence / cleanup / owed-to-siblings are all present and unusually good. Two additions: the branch base `591eb02` is verified current HEAD of `sarol-optimizer-concurrent`; and the "Coordination reality" section should be corrected per *Contract Checks* above (Plan A does touch `adapter.py` and `profiles.py`).

**Coverage of findings §4g:** all nine items map to a phase — 1→1b, 2→1a, 3→1c, 4→0c, 5→0b, 6→0a, 7→Ph2, 8→3a+3b, 9→3c. The ordering claim is also right: NF3's 6-before-5 dependency is real (`adapter.py:907` reads the live verdict path unconditionally, so a stale file is validated as this iteration's prediction), and the archive-then-assert-absent construction does close it. The phase order is sound; the defects above are inside the phases, not in the sequence. Likewise the `profiles.py` 47/48 diagnosis is verified correct — the red is the uncommitted `meta-learnings.md` citing two files by bare filename, it is the optimizer principal's doc hygiene, and consequence (c) ("do not make it green by widening a tree") is exactly the right instinct.

---

## Suggested Revisions

1. Rewrite Phase 0b: cite `adapter.py:907-930`, key the fix on the `UNREADABLE:` violation, and **state the coverage policy** (retry / decrement `requested_count` / tag-but-score). Add a gate asserting a batch with one infra_error still yields a non-zero metric.
2. Fix the verification recipe to `export AGENTIC_LABEL_OPT=…`, re-table the baseline as **444/445** (adapter 150, dispatcher 114, validate_sarol 33, sampling 50, evidence_producers 26, canary 24, profiles 47/48), delete the "phantom baseline" paragraph, and add a check that the engine block is not skipped.
3. Move the minimal checkout to a gold-free root under `/tmp` (Rule 3's shape); add `~/.paper-trail/gold` and `~/.paper-trail/benchmarks` to `inner_scope_problem`'s forbidden set; add gold + benchmark sentinels to V4a; correct NF6 to note `_gold_root()`'s hard-coded fallback.
4. Make V0a two-armed (bypass vs `--permission-prompts none` + non-bypass `--permission-mode` + `--allowedTools`), and re-scope Phase 3 conditionally on the result. Say plainly in *Approach* that retaining `--dangerously-skip-permissions` is a **choice** the plan is testing, not a constraint.
5. Re-cut Phase 3a as consumer-side using the engine's existing `extra_ro_mounts`/`writable_mounts`/`edit_agent_mounts=None`; name `network_policy="open"` + `adc_path=None` + `CLAUDE_CODE_OAUTH_TOKEN`; pin the container CLI to the host's `2.1.267` (or vice versa) and record it in the run manifest.
6. Split V4a into V4a-seal (non-agentic, reuses `run_probe`) and V4a-live (agentic, gated on V0c), and downgrade the "largest single unknown" framing given `open_profile.py:68-72`.
7. Rewrite Phase 2's gate against a **committed isolation pin** (canary pattern), not against what the release builder would stamp.
8. Name the seven `build_components` / `run_baseline.py` callers as lockstep updates; resolve the `run_optimization` vs `build_components` contradiction.
9. Fold `dispatch_prompt.py`'s slot sourcing into a helper shared with `evidence_producers.py`; reuse `validate_sarol`'s violation-code vocabulary; state that the two integrity checks are tautological under `retrieval`.
10. Restate V2c's `$REPO` assertion as an ancestor test plus an exact-set positive; state how V2c observes the `functools.partial`-bound env.
11. Correct the "Coordination reality" section (Plan A touches `adapter.py` `_selftest()` and `profiles.py`); add a `docs/claude_ops.md` §Environment row to *Files to Modify*; add `--bare` to V0b's matrix with its auth and `Task`-tool caveats.
12. Add a V5 note that a VAL **drop** is the expected signature of removing the 61% contamination, so it is not read as a regression.

---

## Questions For The Author

1. **Does the judge need `--dangerously-skip-permissions` at all?** If `--permission-prompts none` + a non-bypass permission mode + an `--allowedTools` set lets the judge complete a staged claim while an out-of-tree read is denied, the app layer becomes a real boundary and Phase 3's role changes from "the seal" to "defence in depth" — which would also resolve OQ3 for a fraction of the cost. This is the highest-leverage unexamined question in the plan.
2. **What is the intended coverage semantics for an infra-failed claim?** A dropped claim currently zeroes the batch. "Retry the claim" and "shrink the denominator" produce different, non-comparable metrics across iterations, so this is a measurement-design decision, not an implementation detail — and Plan A's owed baseline recut runs on whatever is chosen.
3. **Is the missing tag-scoped `.claude/settings.json` + PreToolUse hook stack being deliberately retired, or deferred?** NF1 proves it never existed; `NEXT.md:463` says it should be committed. Retiring it is defensible, but it needs to be a recorded decision rather than a consequence clause — this plan's own root cause is a requirement that was reasoned away in exactly that manner.
4. **Whose baseline is the post-isolation instrument measured against?** The plan contracts Plan A to recut on the post-isolation instrument, but `scripts/run_baseline.py:121` hard-codes `working_checkout=store.repo_root` and would be refused by this plan's own gate. Which script produces the recut, and does it get its own minimal checkout?

---

## Audit Trail

- /home/philadamson/paper-trail/docs/claude_ops.md
- /home/philadamson/paper-trail/docs/session/2026-09-09-optimizer-loop-and-isolation-findings.md
- /home/philadamson/paper-trail/docs/plans/isolation-protocol.md
- /home/philadamson/paper-trail/docs/plans/optimizer-prompt-investigative-latitude.md (branch state via git, not read as prose)
- /home/philadamson/paper-trail/docs/plans/NEXT.md
- /home/philadamson/paper-trail/docs/plans/experiment-sarol-eval-arm-isolation.md
- /home/philadamson/paper-trail/docs/plans/experiment-sarol-optimization-loop-hygiene.md
- /home/philadamson/paper-trail/.claude/commands/sarol-eval-item.md
- /home/philadamson/paper-trail/.claude/ (directory listing), /home/philadamson/paper-trail/.gitignore
- /home/philadamson/paper-trail/experiments/sarol-2024/prompts/adjudicator-dispatch-sarol.md
- /home/philadamson/paper-trail/experiments/sarol-2024/program-v0/manifest.json
- /home/philadamson/paper-trail/experiments/sarol-2024/optimizer/adapter.py
- /home/philadamson/paper-trail/experiments/sarol-2024/optimizer/dispatcher.py
- /home/philadamson/paper-trail/experiments/sarol-2024/optimizer/validate_sarol.py
- /home/philadamson/paper-trail/experiments/sarol-2024/optimizer/canary.py
- /home/philadamson/paper-trail/experiments/sarol-2024/optimizer/evidence_producers.py
- /home/philadamson/paper-trail/experiments/sarol-2024/optimizer/profiles.py
- /home/philadamson/paper-trail/experiments/sarol-2024/scripts/parse_verdict.py
- /home/philadamson/paper-trail/experiments/sarol-2024/scripts/stage_claim.py
- /home/philadamson/paper-trail/experiments/sarol-2024/scripts/freeze_program_v0.py
- /home/philadamson/paper-trail/experiments/sarol-2024/scripts/run_baseline.py
- /home/philadamson/paper-trail/experiments/sarol-2024/scripts/vm/run_hillclimb_vm.sh
- /home/philadamson/paper-trail-planA (git diff / log only)
- /home/philadamson/.paper-trail/engine-82f547d/engine/loop.py
- /home/philadamson/.paper-trail/engine-82f547d/engine/schemas.py
- /home/philadamson/.paper-trail/engine-82f547d/adapter.py
- /home/philadamson/.paper-trail/engine-82f547d/tests/test_envelope_validation.py
- /home/philadamson/.paper-trail/engine-82f547d/tests/test_isolation_negative_control.py
- /home/philadamson/.paper-trail/engine-82f547d/isolation/README.md
- /home/philadamson/.paper-trail/engine-82f547d/isolation/docker_prefix.py
- /home/philadamson/.paper-trail/engine-82f547d/isolation/negative_control.py
- /home/philadamson/.paper-trail/engine-82f547d/isolation/open_profile.py
- /home/philadamson/.paper-trail/engine-82f547d/isolation/Dockerfile
- Runtime probes (read-only): `claude --help`, `claude --version`, `docker info`, `hostname`, `ls ~/.paper-trail/`, and the seven `--selftest` gates under both `PYTHONPATH` and `AGENTIC_LABEL_OPT`

---

## Author's response (2026-09-09) — applied, with one refinement

**All 5 Critical findings and all Failure Modes / Contract Checks / Modularity / Verification /
Handoff items were verified against current code and applied.** Independently re-verified before
applying: the 444/445 baseline under `AGENTIC_LABEL_OPT` vs 258/259 under `PYTHONPATH` (adapter
30/30 → 150/150, dispatcher 48/48 → 114/114); the Scorer's coverage rule at `adapter.py:1378-1382`
and `:1414-1435`; the `invalid_output` classification at `:918-930`; `~/.paper-trail/` holding
`gold`/`benchmarks` as siblings of `runs`; `--permission-prompts none` / `--permission-mode` /
`--bare` present on CLI 2.1.267 vs the Dockerfile's 2.1.218 pin; `_gold_root()`'s hard-coded fallback;
`:549-551` sitting inside `build_components` (`def` at `:495`) with seven callers; `CachingRunner`'s
`enable_cache=True` default; `build_docker_cmd_prefix`'s generic mount params; and the absence of
`engine/adapter.py`.

**One refinement — Failure Mode #2's mechanism.** The review states a fail-closed raise in
`build_components` "turns all seven red — and `run_baseline.py` is what Plan A's owed baseline recut
runs, so this plan's gate would refuse the very recut it contracts Plan A to perform."
`scripts/run_baseline.py:119-121` constructs `adapter.SarolRunner` **directly** and never calls
`build_components`, so the gate would **not refuse** it — it would **not apply to it**. The
underlying point stands and is in fact sharper than framed: this is a silent **gate bypass** leaving
the widest-cwd judge in the system ungated, not a false-positive. Applied in that stronger form, and
raised as **OQ7**.

**Not dismissed:** nothing. No finding was adjudicated in the plan author's favour.
