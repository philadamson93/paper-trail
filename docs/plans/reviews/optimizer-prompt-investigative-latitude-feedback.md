Reference: docs/claude_ops.md

# Feedback: Optimizer prompt: gold as the absolute target, investigative latitude, and a paper-verbatim reset (Codex review)

## Verdict
Blocked. The paper-verbatim reset and most factual spot-checks are sound, but the plan does not implement its promised editable retrieval surface, cannot execute its comparability rule with the current loop, and leaves baseline/tag/hash mechanics and the journal-only path incompatible with the engine.

## Critical Gaps
- Severity: Critical | Gap: Step 3 grants prose authority over query construction, `k`, ranking, and removal of BM25 but adds none of their owners to the program or editable profile. | Why it matters: Today `profiles.py` fixes `retrieval_k=20` and `selector="bm25-top20"`, while `evidence_producers.py` constructs exactly one claim-text query and owns both BM25 constants and ranking code. Optimizer instructions cannot make unreachable code editable, so the plan's central capability grant is false. | Evidence: experiments/sarol-2024/optimizer/profiles.py:103; experiments/sarol-2024/optimizer/profiles.py:107; experiments/sarol-2024/optimizer/evidence_producers.py:53; experiments/sarol-2024/optimizer/evidence_producers.py:92; experiments/sarol-2024/optimizer/evidence_producers.py:183; docs/plans/optimizer-prompt-investigative-latitude.md:139 | Required fix: Specify a manifest-owned retrieval artifact and runtime loading seam (prefer a small declarative config plus a separately scoped implementation only if algorithm replacement is genuinely required), add it to the appropriate profile edit scope, and name all code/tests/docs that load, validate, materialize, version, and report it.
- Severity: Critical | Gap: The mandatory retrieval comparability rule has no executable workflow. | Why it matters: The optimizer cannot run the pipeline itself, and the engine scores the prior committed program under the old condition before the optimizer edits, then scores only the new program after the edit. There is no step that evaluates the previous program under the new retrieval condition, so the required counterfactual cannot be produced within an iteration. | Evidence: docs/plans/optimizer-prompt-investigative-latitude.md:152; experiments/sarol-2024/optimizer/context/edit-surface.md:108; /home/philadamson/.paper-trail/engine-82f547d/engine/loop.py:338; /home/philadamson/.paper-trail/engine-82f547d/engine/loop.py:394; /home/philadamson/.paper-trail/engine-82f547d/engine/loop.py:437 | Required fix: Define who performs the extra counterfactual run, when it occurs, how its cost is budgeted, how the previous program is materialized with the new retrieval condition without changing its judgment files, and how both results are stored. If that requires a factorial program/condition model, state the adapter or engine API change explicitly.
- Severity: Critical | Gap: P0's manifest/hash/tag procedure is incomplete and the baseline tag remains unresolved. | Why it matters: `combined_hash` is generated from committed source refs, not working-tree bytes. Adding two entries requires changing `freeze_program_v0.py`'s `FILESET`, committing the intended bytes, advancing `source_refs`, recomputing every entry hash and `combined_hash`, and verifying the composed tag. A proposed `program-p0` tag conflicts with the engine's canonical contiguous `program-v0..vN` ledger; retaining `program-v0` requires deliberately replacing the existing tag and dealing with existing `program-v1..v5`. | Evidence: experiments/sarol-2024/program-v0/manifest.json:2; experiments/sarol-2024/program-v0/manifest.json:15; experiments/sarol-2024/program-v0/manifest.json:16; experiments/sarol-2024/scripts/freeze_program_v0.py:38; experiments/sarol-2024/scripts/freeze_program_v0.py:83; experiments/sarol-2024/scripts/freeze_program_v0.py:100; /home/philadamson/.paper-trail/engine-82f547d/engine/versioning.py:20; /home/philadamson/.paper-trail/engine-82f547d/engine/versioning.py:88; /home/philadamson/.paper-trail/engine-82f547d/engine/resume.py:92; docs/plans/optimizer-prompt-investigative-latitude.md:89; docs/plans/optimizer-prompt-investigative-latitude.md:255 | Required fix: Resolve D6 before implementation and give an exact migration sequence for the manifest's `program_version`, source refs, old v1-v5 tags/run ledger, new baseline tag, freeze script, dispatcher default/CLI, VM runner, baseline/materialization scripts, and rollback.
- Severity: Critical | Gap: A journal-only iteration is described as complete, but the engine treats it as `no_change`, stops the loop, and never commits the journal. | Why it matters: `commit_new_version` stages only manifest entries and raises `EmptyCommitError` when none changed; `run_loop` converts that to a terminal `LoopStop`. `docs/journal/` is outside the manifest, and the optimizer is explicitly told it cannot commit. The proposed behavior therefore neither completes a normal iteration nor creates the promised committed record. | Evidence: docs/plans/optimizer-prompt-investigative-latitude.md:121; docs/plans/optimizer-prompt-investigative-latitude.md:128; experiments/sarol-2024/optimizer/context/playbook.md:32; experiments/sarol-2024/optimizer/context/edit-surface.md:64; /home/philadamson/.paper-trail/engine-82f547d/engine/versioning.py:99; /home/philadamson/.paper-trail/engine-82f547d/engine/versioning.py:124; /home/philadamson/.paper-trail/engine-82f547d/engine/loop.py:411 | Required fix: Choose and specify semantics: either journal-only is a deliberate terminal stop with a human/harness commit path, or extend the loop/adapter with a typed no-program-change outcome that persists the journal and continues without minting a fake program version. Add a test for that exact path.
- Severity: Critical | Gap: Making the entire slash-command driver optimizer-editable also makes argument parsing, gold/read prohibitions, one-dispatch/no-retry rules, stage topology, output ownership, and abort semantics editable. | Why it matters: Those are measurement and isolation contracts, not classifier quality knobs. An optimizer could improve score or cost by weakening them while remaining inside its declared edit scope; profile stage counts and budget/release claims would then lie. | Evidence: .claude/commands/sarol-eval-item.md:15; .claude/commands/sarol-eval-item.md:35; .claude/commands/sarol-eval-item.md:48; .claude/commands/sarol-eval-item.md:53; .claude/commands/sarol-eval-item.md:123; .claude/commands/sarol-eval-item.md:158; docs/plans/optimizer-prompt-investigative-latitude.md:208 | Required fix: Split the driver into a frozen contract-bearing wrapper and a narrowly editable dispatch/program component, or enumerate immutable invariants and enforce them after every optimizer edit with contract tests. Coordinate this boundary with the isolation sibling before declaring the branches independent.
- Severity: Critical | Gap: The stated ff-only landing path to `main` is impossible from the named base. | Why it matters: The current `sarol-optimizer-concurrent` tip and `main` have diverged; `main` is not an ancestor of `9309434`. A feature branch cut from that tip cannot fast-forward `main`, and the plan does not say how 162 branch-only and 40 main-only commits are reconciled. | Evidence: docs/plans/optimizer-prompt-investigative-latitude.md:298; docs/plans/optimizer-prompt-investigative-latitude.md:306 | Required fix: Name the actual integration target and merge/rebase/cherry-pick sequence, including when the existing optimizer branch and tag ledger are preserved or retired, then give the gate to run after reconciliation.

## Failure Modes
- Optimizer edits a retrieval instruction, but evidence remains BM25 top-20 from the already-imported `profiles.py`/`evidence_producers.py` code | The plan changes prose, not the runtime owner, and the long-lived dispatcher imports both modules before the optimizer turn | Add a materialized, per-version retrieval configuration loader and a negative test proving two configs yield distinct recorded conditions.
- New manifest is hand-edited successfully once, then a later `freeze_program_v0.py --write` silently drops the driver and definitions entries | The generator's eight-entry `FILESET` is omitted from Files to Modify | Add the freeze script and its selftests/docstrings to P0, and require write-then-verify idempotence.
- A fresh run starts from the new definitions but mints `program-v6`, or resume rejects a `program-p0` ledger | Existing `program-v1..v5` tags remain and engine versioning derives the next integer from all canonical tags | Specify tag archival/deletion or use an isolated fresh clone/tag namespace supported end-to-end; verify first commit is the intended v1.
- The canary passes while its committed provenance still names the old `program_combined_hash` | `load()` validates profile, requested model, split, and expected verdict, but never validates the recorded program hash | Re-pin under the new baseline (prefer `--repeat 3`) or make the hash load-bearing; include `canary-retrieval.json` in Files to Modify and assert the recorded hash equals the manifest hash.
- The optimizer edits `verdict_definitions_sarol.md`, but judge behavior does not change, or the definitions drift from the operative rubric | The judge command checks and passes only enum + rubric; the current docs explicitly say the definitions file is not read | State the consumer of the definitions file. Prefer one source of truth referenced from the operative prompt/rubric over two editable copies; otherwise specify lockstep validation.
- Journal, `meta-learnings.md`, and `optimizer/findings/iter-N.md` each acquire the same run defect and prediction history | Step 2 adds a third surface but does not revise the existing authoritative “two records” routing rule or `findings/README.md` | Define mutually exclusive retention scopes and update every routing document in lockstep.
- Reset deletes too much or preserves contradictory v1-v5 prose | “Drop the definitional accretion” does not identify a source tree or exact retained sections beyond the evidence-array rule | Define the reset algorithm explicitly: e.g. start from the `program-v0` versions of both operative files, replace only the eight definitions, reapply the exact evidence-array clause, and list any other retained post-v0 clauses.

## Contract Checks
- Confirmed: the inverted clauses predate optimization. The `program-v0` tag contains the stricter ACCURATE wording and the added OVERSIMPLIFY/NOT_SUBSTANTIATE clauses now visible at `experiments/sarol-2024/specs/verdict_schema_sarol.md:13`.
- Confirmed: `retrieval_k`, `BM25_K1`, `BM25_B`, and query construction are absent from the eight manifest entries. They live in `profiles.py` and `evidence_producers.py`, outside `experiments/sarol-2024/program-v0/manifest.json:17`.
- Confirmed: `.claude/commands/sarol-eval-item.md` is present in the tag's full tree but is not a manifest entry. Consequently the engine does not materialize or version it today, even though the live working checkout supplies it to Claude Code.
- Confirmed with nuance: the canary payload records `program_combined_hash`, but its load key/guards are profile + requested model + split + `expected_verdict`; the hash is provenance only. The plan is right that it does not structurally invalidate, but “re-verify” is insufficient to refresh the stale recorded hash. Evidence: experiments/sarol-2024/optimizer/canary/canary-retrieval.json:2; experiments/sarol-2024/optimizer/canary/canary-retrieval.json:12; experiments/sarol-2024/optimizer/canary.py:115; experiments/sarol-2024/optimizer/canary.py:152; experiments/sarol-2024/optimizer/canary.py:295.
- Confirmed: the output-contract rule is under `### Output contract`; it requires every sub-claim to carry an evidence array and explicitly emits `"evidence": []` for no passage. Evidence: experiments/sarol-2024/prompts/adjudicator-dispatch-sarol.md:99; experiments/sarol-2024/prompts/adjudicator-dispatch-sarol.md:107. The validator schema of record independently requires `sub_claims[*].evidence` at experiments/sarol-2024/optimizer/validate_sarol.py:127.
- The definition text itself is not pinned by adapter/dispatcher selftests. Resetting it should leave the parsed nine-label rollup intact, but adding manifest entries breaks exact count and scope assertions in `adapter.py` and `profiles.py`. Evidence: experiments/sarol-2024/optimizer/adapter.py:1668; experiments/sarol-2024/optimizer/adapter.py:1682; experiments/sarol-2024/optimizer/adapter.py:1767; experiments/sarol-2024/optimizer/profiles.py:261; experiments/sarol-2024/optimizer/profiles.py:275.
- Engine API at stake: `ManifestEntry(path, freeze_policy, content_hash, archive_ref, contract_file, optional)` and `ProgramManifest(entries, combined_hash)`. The adapter-owned `source` and `sha256` extras must continue to be stripped. Adding committed entries is schema-compatible; the engine treats `combined_hash` as opaque and does not verify it. Evidence: /home/philadamson/.paper-trail/engine-82f547d/engine/schemas.py:92; /home/philadamson/.paper-trail/engine-82f547d/engine/schemas.py:112; experiments/sarol-2024/optimizer/adapter.py:188.
- Output vocabulary remains the fixed nine-label enum, so the reset need not migrate scorer labels. The new retrieval condition does need stronger identity fields: `run_manifest.json`, scorer breakdown, VAL allowlist, releases, resume hard fields, and baseline summaries currently carry only `profile`, `retrieval_k`, and model—not query construction, ranker, constants, or a retrieval-condition hash. Evidence: experiments/sarol-2024/optimizer/adapter.py:983; experiments/sarol-2024/optimizer/adapter.py:1415; experiments/sarol-2024/optimizer/adapter.py:1482; experiments/sarol-2024/optimizer/dispatcher.py:818; experiments/sarol-2024/scripts/run_baseline.py:166. Specify exact new keys/types, schema-version impact, and migration behavior for old `0.2.0` releases.
- Engine version pin: the inspected worktree is `agentic-label-opt` at `82f547dac49394005781df62892d41d9b26dfb09`, but `adapter.py` only resolves an environment-selected path and the VM runner checks feature presence rather than the SHA. Evidence: experiments/sarol-2024/optimizer/adapter.py:101; experiments/sarol-2024/optimizer/adapter.py:117; experiments/sarol-2024/scripts/vm/run_hillclimb_vm.sh:42. The plan must either pin/verify `82f547d` or explicitly declare compatible engine features and test that contract; it currently addresses neither.

## Modularity vs. YAGNI
- A real reuse case exists for a versioned retrieval-condition module: the current `retrieval` profile already uses query text, `k`, selector, BM25 constants, and ranking together, and every result/replay needs the same identity. A small schema validated and loaded from each materialized version is warranted. Making arbitrary Python algorithm code optimizer-editable is broader and needs a concrete sandbox/validation story before it is justified.
- `docs/journal/` is established, but the plan's “five entries” claim is stale: this tree contains 14 entries. Existing entries are durable, committed human/agent decision logs and cross-run postmortems; for example, the September postmortem records run-level measurements, defects, and corrective work at docs/journal/2026-09-03-first-optimization-attempt-postmortem.md:5 and :251.
- `optimizer/findings/` is already the detailed per-iteration record: batch metrics, per-claim blames, modes, edits, and predictions, with the next iteration as its named reader. Evidence: experiments/sarol-2024/optimizer/findings/README.md:17; experiments/sarol-2024/optimizer/findings/README.md:29; experiments/sarol-2024/optimizer/findings/README.md:44.
- `meta-learnings.md` is intended for concise cross-iteration technique, but it currently also contains instrument notes and a full iteration log, which already overlaps both other surfaces. Evidence: experiments/sarol-2024/optimizer/meta-learnings.md:181; experiments/sarol-2024/optimizer/meta-learnings.md:202.
- Reuse recommendation: keep `optimizer/findings/` for run-local detail, reduce `meta-learnings.md` to verified reusable optimization heuristics, and reserve `docs/journal/` for curated cross-run decisions/postmortems that a human or landing process promotes. Do not have the optimizer automatically create a journal entry for every single suspected label/session defect; that duplicates findings and bypasses curation. If autonomous durable defect escalation is required, define promotion/deduplication and ownership explicitly rather than silently defaulting to YAGNI.
- The plan duplicates the eight paper definitions in both `verdict_schema_sarol.md` and `verdict_definitions_sarol.md` while making both editable. Because the judge reads only the former, the latter becomes either inert or a second drifting authority. Reuse one definitions source or state distinct roles and enforce consistency.

## Verification Gaps
- Step C's expected suite counts assume the old eight-entry manifest. Updating only the listed files will fail adapter and profiles selftests; the plan must name their exact assertion changes and add negative controls showing the two new files are materialized, editable only under intended profiles, committed into v1+, and caught by tag/tree drift checks.
- The six suites are not currently all green in this checkout: `sampling.py --selftest` reports 48/50 because its real staging seam fails, and `profiles.py --selftest` reports 47/48 because two backticked doc paths do not resolve from repo root. Evidence for the failing gates: experiments/sarol-2024/optimizer/sampling.py:696; experiments/sarol-2024/optimizer/profiles.py:349. Record these as pre-existing blockers or repair them in a separately scoped prerequisite; “any suite regresses” is not an actionable baseline while red is pre-existing.
- No verification proves retrieval actually changed. Add a deterministic fixture that varies query/`k`/ranker configuration and asserts different selected passages, a changed program identity, and complete provenance in run manifest/release/baseline summary.
- No verification proves the counterfactual comparison required by Step 3, including same roster, old judgment program + new retrieval condition, distinct artifacts, and cost accounting.
- Step D requires the optimizer to open a trace, but `trace_ref` may legitimately be null and correct-answer traces are available only through run manifests, whereas the mistake corpus lists only errors. State fallback behavior and the exact artifact used to sample correct traces. Evidence: experiments/sarol-2024/optimizer/context/release-format.md:93; experiments/sarol-2024/optimizer/context/release-format.md:154; experiments/sarol-2024/optimizer/context/release-format.md:170.
- The live probe success criterion tests documentation behavior but not preservation of the `"evidence": []` rule on an empty-window fixture. Add an offline validator regression and a one-claim empty-window smoke before paid verification.
- The canary success criterion should assert its payload's `program_combined_hash` equals the new manifest hash, not merely that the observed verdict remains ACCURATE.
- The plan cites the verbatim paper wording only through a git-ignored session document that it later intends to retire. Add a stable primary-source citation (paper identifier/URL, section, Table 1, access date) to the plan or durable reconciliation journal so Step A remains reproducible after cleanup.

## Handoff Readiness
- Files and line ranges: most prose destinations are named but no insertion/replacement ranges are given beyond a few stale point references. P0 does not identify the exact source revision for the reset or enumerate which v1-v5 passages survive. Step 3 omits all actual retrieval owners. The new journal file does not name a closest sister file; `docs/journal/2026-09-03-first-optimization-attempt-postmortem.md` is the closest run-level precedent.
- Schema of record: the plan names the manifest but not its generator as authoritative. It must state exact manifest entry objects for the driver, definitions, and retrieval artifact (`path`, `source`, `freeze_policy`, `contract_file`, `optional`, `sha256`) and update `source_refs`/`program_version`. Evidence: experiments/sarol-2024/program-v0/manifest.json:17.
- Cross-stage lockstep: add `freeze_program_v0.py`, `profiles.py`, `adapter.py` selftests, `profiles.py` doc gates, `findings/README.md`, `canary-retrieval.json`, `run_baseline.py`, `materialize_smoke.py`, `run_hillclimb_vm.sh`, and likely `dispatcher.py` to Files to Modify. If retrieval identity grows, also add release schema/version and resume compatibility surfaces.
- Concrete success: prose behavior criteria are useful, but no acceptance criterion establishes that retrieval is reachable or comparisons are unconfounded. Add deterministic program-identity, retrieval-output, provenance, and counterfactual-run assertions before the paid probe.
- Out of scope: objective redesign, eval resize, and model swap are explicit. Engine changes, tag-ledger migration, isolation invariants of the editable driver, and automated journal persistence are not clearly in or out; they must be dispositioned because the proposed design depends on them.
- Coordination: the plan calls the isolation sibling independent even though both alter the same driver and its read/dispatch boundary. Define merge order and conflict owner. Also reconcile the dirty working tree's untracked findings/plans and modified `meta-learnings.md` before branch creation without folding run artifacts accidentally into the baseline commit.
- Landing: D6 is still open, the base cannot ff-only to `main`, old canonical version tags remain, and no cleanup/migration is given for prior run summaries/materialized snapshots. The landing gate must be rewritten around the chosen integration branch and baseline ledger.
- Resolved “Open Questions” D1-D4 should move into a Decisions section or the relevant approach steps. D5 and D6 are genuinely open, yet the plan is otherwise presented as handoff-ready and requires them at the landing gate; D6 must be resolved before implementation, while D5 should be explicitly deferred or scoped as verification.
- Dangling/unstable references: `findings §8` is git-ignored and scheduled for retirement; “rad-eval mirrors” has no repository/commit/path; `claims-train.jsonl` has no full path; and the queued isolation plan has no concrete plan path. Replace each load-bearing reference with a durable exact pointer.

## Suggested Revisions
- Split the work into two sequenced plans or gates: (1) paper-verbatim artifact reset + canonical baseline recut; (2) optimizer latitude, retrieval programmability, comparability, and journal semantics. The first can land without pretending the second is prose-only.
- Define P0 mechanically from a named source tree, update the freeze generator before the generated manifest, commit all baseline bytes, regenerate from committed refs, re-pin the canary, verify materialization, and cut the one canonical tag chosen after resolving the old ledger.
- Introduce a versioned retrieval-condition schema with exact fields such as `query_strategy`, `k`, `ranker`, `bm25_k1`, `bm25_b`, and a condition hash/version. Load it from `materialized_path` for every Runner call; propagate the identity through attestation, run manifests, releases, baseline summaries, and resume hard fields.
- Specify a counterfactual evaluator that pairs old judgment artifacts with the new retrieval condition, including artifact naming and budget. If this is too much for the current plan, keep retrieval fixed and remove the “fully editable” claim until the dedicated design lands.
- Keep the command's safety/orchestration shell frozen. Move only evidence- and judgment-policy content into manifest-owned editable files, with post-edit invariant tests for one dispatch, no retry, no gold/source overreach, output ownership, and stage/profile consistency.
- Replace the three-record ambiguity with a promotion model: findings are automatic per-iteration scratch, meta-learnings are verified reusable heuristics, journal entries are curated durable cross-run decisions/defects. State who commits promotions and how duplicates are avoided.
- Rewrite Landing & cleanup after checking actual branch ancestry: name the integration branch, merge strategy, exact tag migration, isolation-plan ordering, preservation of dirty artifacts, post-merge gates, and rollback.

## Questions For The Author
- Should the reset replace/re-cut canonical `program-v0` and archive the old v0-v5 ledger, or should a new experiment namespace/tag prefix be introduced? `program-p0` cannot be dropped into the current engine's resume/versioning conventions without more changes.
- Is retrieval programmability meant to cover only parameters/query construction, or arbitrary ranking implementations including removing BM25? The latter materially widens executable-code and sandbox scope.
- When retrieval changes, do you want a true 2×2 comparison (old/new judgment × old/new retrieval), or only old judgment re-run under the new retrieval condition? Who pays and records it?
- Should `verdict_definitions_sarol.md` become an operative judge input, an optimizer-only reference, or remain a frozen provenance record? Making it editable without naming its consumer creates a second silent rubric.
- Is a journal-only result supposed to stop the run for human triage, or should the loop continue? The current engine can only do the former.
- Which driver clauses are legitimately optimizer-editable, and which are immutable measurement/isolation contracts shared with the queued isolation plan?
- Should the two pre-existing failing offline gates be prerequisites for this change or tracked as accepted baseline failures with explicit issue references?

## Audit Trail
- Files inspected (paths only)
  - docs/claude_ops.md
  - docs/session/2026-09-09-optimizer-loop-and-isolation-findings.md
  - docs/plans/optimizer-prompt-investigative-latitude.md
  - docs/plans/NEXT.md
  - docs/plans/README.md
  - docs/journal/2026-04-21-tiered-leakage-framework-decisions.md
  - docs/journal/2026-04-22-topology-freedom-and-optimizer-affordances.md
  - docs/journal/2026-09-03-first-optimization-attempt-postmortem.md
  - .gitignore
  - .claude/commands/sarol-eval-item.md
  - experiments/sarol-2024/program-v0/manifest.json
  - experiments/sarol-2024/prompts/adjudicator-dispatch-sarol.md
  - experiments/sarol-2024/specs/verdict_enum_sarol.md
  - experiments/sarol-2024/specs/verdict_schema_sarol.md
  - experiments/sarol-2024/specs/verdict_definitions_sarol.md
  - experiments/sarol-2024/scripts/freeze_program_v0.py
  - experiments/sarol-2024/scripts/materialize_smoke.py
  - experiments/sarol-2024/scripts/run_baseline.py
  - experiments/sarol-2024/scripts/vm/run_hillclimb_vm.sh
  - experiments/sarol-2024/optimizer/README.md
  - experiments/sarol-2024/optimizer/adapter.py
  - experiments/sarol-2024/optimizer/canary.py
  - experiments/sarol-2024/optimizer/canary/canary-retrieval.json
  - experiments/sarol-2024/optimizer/dispatcher.py
  - experiments/sarol-2024/optimizer/evidence_producers.py
  - experiments/sarol-2024/optimizer/profiles.py
  - experiments/sarol-2024/optimizer/sampling.py
  - experiments/sarol-2024/optimizer/validate_sarol.py
  - experiments/sarol-2024/optimizer/prompt/optimizer-instructions.md
  - experiments/sarol-2024/optimizer/context/edit-surface.md
  - experiments/sarol-2024/optimizer/context/failure-mode-discovery.md
  - experiments/sarol-2024/optimizer/context/playbook.md
  - experiments/sarol-2024/optimizer/context/release-format.md
  - experiments/sarol-2024/optimizer/context/subagent-blame-brief.md
  - experiments/sarol-2024/optimizer/context/task-and-scoring.md
  - experiments/sarol-2024/optimizer/findings/README.md
  - experiments/sarol-2024/optimizer/meta-learnings.md
  - /home/philadamson/.paper-trail/engine-82f547d/engine/schemas.py
  - /home/philadamson/.paper-trail/engine-82f547d/engine/materialize.py
  - /home/philadamson/.paper-trail/engine-82f547d/engine/versioning.py
  - /home/philadamson/.paper-trail/engine-82f547d/engine/resume.py
  - /home/philadamson/.paper-trail/engine-82f547d/engine/loop.py

## Adjudication (added by Claude after applying this review, 2026-09-09)
- **Applied:** all six Critical gaps, all seven Failure Modes, the Verification Gaps, and the Handoff
  Readiness items. The plan was split into Plan A (reset + baseline recut + prose latitude) and Plan B
  (`docs/plans/phase2-evidence-acquisition-programmability.md`), per Suggested Revision #1.
- **DISAGREED — `sampling.py --selftest` is not red.** The review reports 48/50 with "its real staging
  seam fails". Measured on this checkout: **50/50 passed** under both `python3.10` and
  `~/.local/bin/python3.13`. The likely cause is the reviewer's own `-s workspace-write` sandbox
  blocking the staging write. `profiles.py --selftest` at **47/48** is confirmed — the failure is the
  `_unresolvable_doc_paths` gate (`profiles.py:349`) tripping on template placeholders (`iter-<n>.md`)
  and README-relative paths. Plan A records the measured baseline rather than "all six green".
- **DISAGREED on the FIX, agreed on the FINDING — Critical #1 (retrieval).** The finding is correct:
  prose cannot make `profiles.py`/`evidence_producers.py` editable, so the earlier Step 3 was false.
  But the proposed fix (a manifest-owned retrieval-condition schema with `query_strategy`/`k`/`ranker`/
  `bm25_k1`/`bm25_b`) is declined. `profiles.py:121-133` already grants `ACQUISITION_SCOPE` — the
  extractor and verifier prompts, already manifest-owned and already optimizer-editable — on the
  `agentic`/`paperclip` profiles, and `profiles.py:59-61` shows the real blocker is that the driver
  aborts those stages with `STAGE_NOT_IMPLEMENTED`. The `retrieval` profile's BM25 fixity is a
  deliberate Phase-1 isolation of judgment from acquisition. A config schema would engineer around
  that constraint and duplicate a Phase-2 capability. Plan B pursues driver stage coverage instead.
- **Confirmed independently:** ff-only impossible (`main` not an ancestor of `9309434`; 162 branch-only
  vs 40 main-only commits); `docs/journal/` holds 14 entries, not the five the plan claimed;
  `docs/plans/NEXT.md` exists.

