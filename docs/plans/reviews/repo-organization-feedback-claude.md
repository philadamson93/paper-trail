Reference: docs/claude_ops.md

# Feedback: Agent-instruction-forward repo organization (Fresh Claude Code review)

## Verdict
Revise. The plan is structurally sound and RD-1..RD-8 are well-reasoned, but two load-bearing details are wrong against the current repo: (a) the migration-manifest grep commands the plan recommends do not work as specified (one matches nothing, the other errors out), and (b) `paperclip/SKILL.md` is currently **gitignored**, not part of the shipped surface — RD-3's framing and the `.gitignore` rewrite plan need to acknowledge that.

## Critical Gaps

- Severity: Critical | The plan's recommended migration-manifest grep `git grep -nE '\.claude/|^\s*templates/'` silently misses every real `templates/` reference, because none of them begin with whitespace. Verified: that exact command emits zero `templates/` hits, while `git grep -nE 'templates/'` returns 20+ legitimate hits across `CLAUDE.md`, `README.md`, `docs/claude_ops.md`, `.claude/commands/{paper-trail,init-writing-tools}.md`, multiple `docs/plans/*.md`, and others. | Evidence: `docs/plans/repo-organization.md:253` (Codebase pointers — the manifest command); `docs/plans/repo-organization.md:88` (the variant in Files to update). | Required fix: replace `^\s*templates/` with `(^|[^[:alnum:]_/])templates/` or simpler — `git grep -nE '\.claude/|(\b|/)templates/' -- ':!docs/journal/*' ':!docs/plans/reviews/*'` — and verify it returns at least the 20+ hits above before treating it as the canonical manifest.

- Severity: Critical | The variant grep in "Files to update" at `docs/plans/repo-organization.md:88` uses PCRE negative-lookahead: `templates/(?!claims_ledger\.md$)`. Verified: `git grep -E` rejects this with `fatal: command line, 'templates/(?!claims_ledger\.md$)': repetition-operator operand invalid`. The plan tells the implementer to "generate the canonical manifest at implementation time" with a command that errors out. | Evidence: `docs/plans/repo-organization.md:88` | Required fix: drop the negative-lookahead (POSIX ERE doesn't support it) and either pass `-P` for PCRE, or simpler — just match all `templates/` hits and let the implementer eyeball the `claims_ledger.md` ones. Also worth noting: excluding `templates/claims_ledger.md` from the manifest is exactly wrong, because that file IS the one being moved to `src/templates/claims_ledger.md` and every reference needs to follow.

- Severity: Critical | RD-3 treats `paperclip/SKILL.md` as part of "the mirrored ship surface" but the file is currently **gitignored** (`git check-ignore` confirms; `git ls-files .claude/skills/` returns only `doc-split-check.md` and `plan-check.md`). The `.gitignore` carveout block at `.gitignore:25-27` carves out exactly those two files and not `paperclip/SKILL.md`. After the plan's `.gitignore` rewrite (remove `.claude/skills/*` block, rely on `src/skills/` being tracked), `paperclip/SKILL.md` transitions from untracked to tracked — a meaningful behavior change that's not called out in the plan. | Evidence: `.gitignore:24-27`; `git ls-files .claude/skills/` returns 2 paths; `git check-ignore -v .claude/skills/paperclip/SKILL.md` matches `.gitignore:25`. | Required fix: in the Implementation surface bullet at `docs/plans/repo-organization.md:80-84` and in RD-3, add an explicit subnote: "today `paperclip/SKILL.md` is untracked (gitignored); after the move it becomes tracked at `src/skills/paperclip/SKILL.md`. Confirm with the user that the current `paperclip/SKILL.md` contents are intended to ship before committing the move." Otherwise the move silently flips a 208-line file from local-only to shipped.

## Failure Modes

- Scenario: Implementer follows the plan literally and runs `git grep -nE '\.claude/|^\s*templates/'` as the canonical manifest. | Why the plan misses it: see Critical #1; the manifest is silently incomplete and downstream "Repo-wide path-policy sweep" at line 280 will pass while real stale references stay in CLAUDE.md, README.md, and `.claude/commands/paper-trail.md` line 14. | What to add: a smoke step that asserts the manifest contains ≥ N hits across ≥ M files (calibrated against today's tree).

- Scenario: Fresh `git clone` on Linux of paper-trail; user opens in Claude Code. The `.claude/commands/` symlink resolves to `../src/commands/` and slash-command discovery either works or doesn't — but the plan never tests this on a fresh clone, only in-place. | Why the plan misses it: the smoke test at lines 130-136 vendors the repo into `/tmp/vendor-smoke` but uses `cp -r`, not `git clone`. `cp -r` of a symlink behavior is platform-specific (BSD vs GNU cp); `git clone` deterministically stores symlinks per `git config core.symlinks`. A user on a system with `core.symlinks=false` (Windows, or some CI containers) gets stub text files, not symlinks. The plan rules Windows out of scope but doesn't rule out non-Windows machines that happen to have symlinks disabled. | What to add: a `git clone` (not `cp -r`) smoke step into a clean directory + verify `readlink .claude/commands` returns `../src/commands` and not a literal path text file.

- Scenario: Author-mode vendoring smoke test passes at step 4 (slash command appears), but `/paper-trail --author` fails at step 5 because the vendored `.claude/commands/paper-trail.md` references `src/specs/verdict_schema.md` (per RD-2) and the vendored writing repo's symlink resolves against the vendored `src/specs/...` correctly — but only if `cp -r` followed the symlink and copied the target. On many systems `cp -r` of the source repo's `.claude/` copies the symlinks as symlinks (relative path `../src/commands/`), which then resolve against the writing repo's `src/` — only if the user copied `src/` first and the relative path is preserved. | Why the plan misses it: the README phrasing "copy `src/`, `.claude/`, and `templates/` (in that order — `.claude/` contains symlinks that must resolve against the vendored `src/`)" in RD-1 implies ordering matters but doesn't actually describe what behavior depends on order. `cp -r .claude` copies symlinks-as-symlinks by default on macOS/Linux; whether the vendored symlinks resolve depends on whether `src/` exists at the destination at resolution time (it does, since slash-command discovery happens later), not at copy time. The "ordering" advice is folklore. | What to add: spell out in the smoke test that the post-copy `readlink /tmp/vendor-smoke/.claude/commands` must equal `../src/commands` (relative), not the source repo's absolute path. If it's absolute, every vendored repo silently reads from the source paper-trail checkout — a working-by-accident outcome that breaks when the source repo is moved.

- Scenario: Brevity audit (RD-5) moves a "reference material" block out of `paper-trail.md` and into `src/specs/verdict_schema.md`. The new pointer in `paper-trail.md` references `src/specs/verdict_schema.md`. The orchestrator runs through the symlink as `.claude/commands/paper-trail.md`. Either reference (`src/specs/...` per RD-2 or `.claude/specs/...`) resolves to the same file via the symlink chain — but a vendored author-mode repo may have `src/` colocated with the symlinks, in which case the `src/specs/...` path in the dispatch prompt sent to a subagent is **relative to the agent's cwd**, not relative to the repo root. | Why the plan misses it: dispatch prompts contain literal path strings (`src/specs/verdict_schema.md`) that subagents resolve against their working directory. In reader mode the cwd is `<output-dir>`; in author mode it's the writing repo. There's no guarantee `src/specs/verdict_schema.md` resolves from those cwds; the symlink only redirects `.claude/specs/...`, not `src/...`. | What to add: a verification step that one dispatch prompt's spec-path reference (e.g., the `verdict_schema.md` reference inside `extractor-dispatch.md` line 58 and `adjudicator-dispatch.md` lines 20 & 75) is actually resolvable from the subagent's working directory in both reader and author modes — or a decision that those references stay as absolute (or stay as `.claude/specs/...`) regardless of RD-2's "single source of truth" goal. RD-2 is a documentation principle that has runtime implications the plan doesn't trace.

## Contract Checks

- **Author-mode vendoring contract (cross-repo).** The smoke test at lines 130-136 covers `cp -r`, slash-command discovery, and one `/paper-trail --author` invocation. Missing: (a) `readlink` check on the vendored symlinks to catch relative-vs-absolute symlink targets; (b) clean `git status` in the *vendored* directory (a user who runs `git init` in their writing repo and copies paper-trail in shouldn't get a `git status` polluted with the vendored tree as untracked, but that's their problem — what's *our* problem is whether the symlinks survive `git add .` in the writing repo. Some users will commit the vendored layout, and a `.claude/commands/` symlink pointing to `../src/commands/` works in their repo as long as `src/` is alongside; commit it). | The README change in RD-1 should explicitly note that vendoring users who commit the layout will be committing symlinks, and git's `core.symlinks` config affects whether other clones of *their* repo resolve correctly.

- **In-repo path-policy contract (RD-2).** "Every shipped runtime reference migrates to `src/...`; `.claude/` is publication-only." The verification at line 280 is `git grep -nE '\.claude/' -- 'src/' 'README.md' 'docs/' ':!docs/journal/*' ':!docs/plans/reviews/*'`, expecting empty output. Missing: this regex excludes `CLAUDE.md` and `.gitignore` from the sweep. `CLAUDE.md:18-22, 35-38, 65, 74-75` currently has 10+ `.claude/` references. Some are intentional per RD-2 (the `.gitignore` carveout, the "publication target" note), but the sweep should explicitly cover `CLAUDE.md` and either migrate or annotate inline. | Add `CLAUDE.md` to the sweep targets at line 280.

- **Subagent-context contract (RD-5 brevity audit).** "Move reference material to specs regardless of line count" on `paper-trail.md`. Missing: a check that moved content doesn't bloat the **subagent** context — when a dispatch prompt's reference to a spec causes the subagent to read the spec, the spec becomes part of the subagent's working set. Moving 50 lines out of `paper-trail.md` into `verdict_schema.md` is a net-negative for the orchestrator's context and a net-zero for the adjudicator subagent (who reads `verdict_schema.md` anyway), but a net-negative for the extractor subagent if the moved block is irrelevant to extraction. | Add an audit check: for each block moved into a spec, name which subagents read that spec.

## Modularity vs. YAGNI

- **`src/templates/` for one file (RD-6).** Plan's choice: keep as directory. Modular alternative: flatten to `src/claims_ledger.md` if only one file ever lives there. Realistic use case the plan names: `feature-issue-command.md` implies `issue-draft.json` arrives later. | Recommendation: agree with RD-6 — the alternative saves one directory level for negative payoff (re-creating the directory later is cheap but the rename touches references). Specifically, `docs/plans/feature-issue-command.md` references suggest a second template is imminent enough that the directory pays off.

- **`src/specs/control_flow.md` as a new artifact.** Plan's choice: human-maintained spec, single source of truth for the dispatch graph. Reuse-vs-rewrite check: the orchestrator-enforced validation rules already exist at `.claude/specs/verdict_schema.md:179-193` ("Validation rules (orchestrator-enforced)" — verified). Per-subagent JSON shapes are in `verdict_schema.md`. Phase ordering and dispatch sites are in `.claude/commands/paper-trail.md` (Step 3.2, line 380+). The new file consolidates these into a pathway-table view — that's net additive (the existing artifacts state the rules in their domain; `control_flow.md` cross-cuts the graph). Risk: drift if `control_flow.md` re-states schema rules rather than pointing at them. | Recommendation: agree with the plan. The "Where validation actually lives" paragraph at lines 179-180 already addresses this risk explicitly. Add a stronger constraint to the implementer: `control_flow.md` MUST point at `verdict_schema.md` "Validation rules" by anchor — never reproduce the bullets — and any pathway-row Exit-validation cell that needs more than a sentence must link to the source spec rather than expand.

- **Symlink subdirectories vs `make sync` (RD-4).** Plan's choice: symlinks first, defined fallback. Modular alternative: skip the symlink experiment, go straight to `make sync` for predictability. Realistic concern: symlinks fail silently on edge platforms (some CI runners, Docker volume mounts, Windows WSL with case-insensitive filesystems). | Recommendation: agree with RD-4 — the `claude_ops.md` line 22 "no build step" assumption is load-bearing for the dev-loop ergonomics, and symlinks preserve it. But raise to user: the fallback in RD-4 ("update `docs/claude_ops.md`'s 'no build step' assumption" via a follow-up commit) is hand-wavy. If the symlink smoke test fails, the implementer is being asked to make a non-trivial architectural change mid-plan. Pre-write the `claude_ops.md` diff for the fallback case so the choice is "ship the symlinks" or "apply the prepared fallback patch," not "design a fallback under pressure."

## Verification Gaps

- The "Repo-wide path-policy sweep" at line 280 will not catch stale `templates/` references — same root cause as Critical #1 (the regex is `.claude/` only, not `templates/`). After the move, files referencing the old top-level `templates/claims_ledger.md` instead of `src/templates/claims_ledger.md` will pass the sweep and ship broken.

- The `git log --follow` check at line 281 verifies history continuity for ONE representative directory-shaped skill (`paperclip/SKILL.md`). But that file is currently **untracked**, so `git log --follow src/skills/paperclip/SKILL.md` after the move will return empty history — not because `git mv` failed, but because there was no history to follow. The check will pass coincidentally. | Replace the check target with a known-tracked file: `git log --follow src/skills/plan-check.md` or `src/skills/doc-split-check.md` for the single-file shape. For the directory-shaped skill specifically, the test of `git mv` preserving history is moot until the file is first committed; flag this gap.

- The "Slash-command discovery" verification at line 276 is structural: "Claude Code can't find the slash command." Behavioral version: "Claude Code can find AND successfully execute the slash command end-to-end (Phase 0 through Phase 5)." The plan's smoke step (d) at line 15 covers the end-to-end run, but the bulleted verification at line 276 only requires discovery, not execution. The two-tier coverage is fine — but RD-4's fallback trigger is "Claude Code discovery fails in practice" (line 294), which is structural. A subtle execution-time failure (e.g., the symlink resolves for discovery but a slash-command lookup inside the running subagent fails) would not trigger the fallback. | Tighten RD-4's fallback trigger to include "or a behavioral smoke step (`/paper-trail` end-to-end on `examples/paper-trail-adamson-2025/`) fails for a discovery-or-symlink-related reason."

- No verification that `settings.local.json` (the only file plan-confirmed to stay at `.claude/` root) is excluded from the migration. The plan asserts at line 65 that it stays canonical; no test asserts that after `git mv`, the file's path is exactly `.claude/settings.local.json` and not `src/settings.local.json` or anywhere else. Add: `test -f .claude/settings.local.json && test ! -e src/settings.local.json`.

## Suggested Revisions

- At `docs/plans/repo-organization.md:88` and `:253`: fix both grep patterns. The plan should run them itself in the planning-mode preview before declaring the manifest. (I ran them; both fail. The author can reproduce in 5 seconds: `cd /Users/philadamson/Documents/Misc/Projects/paper-trail && git grep -nE '\.claude/|^\s*templates/' -- ':!docs/journal/*' ':!docs/plans/reviews/*' | grep -c templates/` returns 0.)

- At `docs/plans/repo-organization.md:80` ("Files to move"): add explicit subnote that `paperclip/SKILL.md` is currently untracked; flag the transition to tracked + ask the user whether the 208-line file is intended to ship.

- At `docs/plans/repo-organization.md:96` (`.gitignore` update): the plan says "Remove the `.claude/skills/*` ignore line and its carveouts." Correct — but the post-move `.gitignore` should keep ignoring **machine-local** auto-installed skills at `.claude/skills/<other-name>.md` if Claude Code auto-installs into the symlink target. Concretely: after the symlink, if Claude Code creates `.claude/skills/foo.md` it actually creates `src/skills/foo.md`, which is tracked. That's a behavior change worth noting — the plan should either accept that all auto-installed skills become tracked, or move `.claude/skills/` out of the symlink set (back to a real directory that ignores everything except mirrors of `src/skills/`).

- At `docs/plans/repo-organization.md:280` ("Repo-wide path-policy sweep"): broaden the sweep regex to cover `templates/` and broaden the target set to include `CLAUDE.md` and `.gitignore` explicitly.

- At `docs/plans/repo-organization.md:281` (`git log --follow` check): replace `src/skills/paperclip/SKILL.md` with a tracked file. Add a separate note that the paperclip skill's git history starts at the repo-org commit.

- Pre-write the RD-4 fallback `claude_ops.md` diff so the fork at smoke-test time is "apply prepared patch" not "improvise." The current claude_ops.md "no build step" assertion is at `docs/claude_ops.md:22`.

- In the dispatch-prompt path migration (`docs/plans/repo-organization.md:148-153`): clarify whether `src/specs/verdict_schema.md` references in `extractor-dispatch.md` / `adjudicator-dispatch.md` are intended to be resolved relative to the subagent's cwd. If yes, document the cwd contract; if no, leave them as `.claude/specs/...` for the symlink to resolve. RD-2 is ambiguous on this runtime question.

## Questions For The Author

- Is `paperclip/SKILL.md` intended to ship? The current state (gitignored, no carveout) suggests no, but RD-3 assumes yes. Resolving this changes whether the reorg commits the 208-line file or carves it out explicitly in the new `.gitignore`.

- For author-mode vendoring: is the contract "user copies the source tree into their writing repo and uses it locally," or "user copies and commits the layout to their writing repo's git history"? The two have different symlink ergonomics (absolute vs relative target, `core.symlinks` portability). The README rewrite should be explicit.

- RD-2 says shipped prompts/specs reference `src/...` for single source of truth. Do those references need to *resolve* at runtime (from the subagent's cwd), or are they purely documentation? The dispatch prompts at `.claude/prompts/extractor-dispatch.md:58` and `:adjudicator-dispatch.md:20,75` reference `.claude/specs/verdict_schema.md` today; after RD-2 they reference `src/specs/verdict_schema.md`. A subagent that tries to actually open that path will get different behavior depending on cwd.

## Audit Trail

- /Users/philadamson/Documents/Misc/Projects/paper-trail/docs/claude_ops.md
- /Users/philadamson/Documents/Misc/Projects/paper-trail/docs/plans/repo-organization.md
- /Users/philadamson/Documents/Misc/Projects/paper-trail/.claude/ (directory inventory: commands/, prompts/, specs/, scripts/, skills/, settings.local.json)
- /Users/philadamson/Documents/Misc/Projects/paper-trail/.claude/skills/ (doc-split-check.md, plan-check.md, paperclip/SKILL.md)
- /Users/philadamson/Documents/Misc/Projects/paper-trail/.claude/skills/paperclip/SKILL.md (first 10 lines + line count)
- /Users/philadamson/Documents/Misc/Projects/paper-trail/.claude/commands/ (line counts verified for all 6 files)
- /Users/philadamson/Documents/Misc/Projects/paper-trail/.claude/prompts/ (line counts verified)
- /Users/philadamson/Documents/Misc/Projects/paper-trail/.claude/specs/verdict_schema.md (Validation rules section at line 179-193)
- /Users/philadamson/Documents/Misc/Projects/paper-trail/.claude/scripts/validate_claims.py (header + manuscript-validator confirmation)
- /Users/philadamson/Documents/Misc/Projects/paper-trail/.claude/commands/paper-trail.md (Step 3.2 / 3.5 dispatch sites verified, lines 352, 360, 383, 411, 445, 512)
- /Users/philadamson/Documents/Misc/Projects/paper-trail/templates/claims_ledger.md (presence verified)
- /Users/philadamson/Documents/Misc/Projects/paper-trail/.gitignore (carveout pattern verified — only doc-split-check.md and plan-check.md)
- /Users/philadamson/Documents/Misc/Projects/paper-trail/README.md (vendor-copy section at lines 60-90)
- /Users/philadamson/Documents/Misc/Projects/paper-trail/CLAUDE.md (Where things live + reading path)
- /Users/philadamson/Documents/Misc/Projects/paper-trail/docs/plans/feature-paperclip-first-architecture.md (.claude/<dir>/<file> references confirmed)
- Migration manifest greps executed: `git grep -nE '\.claude/|^\s*templates/'` (0 templates/ hits) and `git grep -nE '\.claude/|templates/(?!claims_ledger\.md$)'` (fatal error)
- `git check-ignore -v .claude/skills/paperclip/SKILL.md` (matched .gitignore:25)
- `git ls-files .claude/skills/` (returned doc-split-check.md and plan-check.md only)
